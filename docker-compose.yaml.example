version: "3.7"

services:
  snapshotter:
    build: .
    container_name: snapshotter
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./snapshots:/snapshots
      - ./errors:/errors
